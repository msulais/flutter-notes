enum Routes {
    home,
    archive,
    trash
}

enum SortBy {
    title,
    label,
    image,
    dateCreated,
    dateModified
}

enum SortMode {
    ascending,
    descending
}

enum SettingsKey {
    useDynamicColor,
    theme,
    color,
    sortBy,
    sortMode
}